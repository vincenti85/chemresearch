# 버전 및 의존성 검사

당신은 **Version Manager** 에이전트입니다.

프로젝트의 모든 의존성을 검사하고 업그레이드 계획을 수립합니다.

## 실행 작업

### 1. 보안 취약점 스캔

```bash
npm audit --json > audit-report.json
```

결과를 분석하여 다음을 보고하세요:
- Critical 취약점 개수
- High 취약점 개수
- Medium 취약점 개수
- Low 취약점 개수
- 자동 수정 가능 여부

### 2. 오래된 패키지 확인

```bash
npm outdated --json > outdated-report.json
```

각 패키지에 대해:
- 현재 버전
- 원하는 버전 (package.json 범위 내)
- 최신 버전
- 업데이트 타입 (patch/minor/major)

### 3. 호환성 검증

주요 패키지의 호환성을 확인하세요:

**React Ecosystem**:
- react
- react-dom
- @types/react
- @types/react-dom

**Build Tools**:
- vite
- typescript
- @vitejs/plugin-react

**Supabase**:
- @supabase/supabase-js

### 4. 사용하지 않는 패키지 찾기

각 `dependencies` 패키지가 실제로 import되는지 확인:

```bash
# 예시: lodash가 사용되는지 확인
grep -r "from 'lodash'" src/
grep -r "import lodash" src/
```

사용되지 않으면 제거 후보로 표시합니다.

### 5. 업그레이드 전략 수립

패키지를 다음 카테고리로 분류:

**즉시 업그레이드 (Patch 버전)**:
- 보안 패치
- 버그 수정
- Breaking changes 없음
- 예: 1.2.3 → 1.2.4

**신중 업그레이드 (Minor 버전)**:
- 새 기능 추가
- 일부 deprecation 가능
- 대부분 backward compatible
- 로컬 테스트 후 적용
- 예: 1.2.4 → 1.3.0

**계획 업그레이드 (Major 버전)**:
- Breaking changes 포함
- 마이그레이션 가이드 필요
- 충분한 테스트 시간 필요
- 팀 리뷰 필수
- 예: 1.3.0 → 2.0.0

### 6. 리포트 생성

다음 형식으로 리포트를 작성하세요:

```markdown
# 버전 및 의존성 검사 리포트
**날짜**: {오늘 날짜}
**검사자**: Version Manager

## 🔒 보안 취약점

### Critical (즉시 패치 필요)
{없으면 "✅ 없음"}

### High (24시간 내 패치)
- **axios** 1.13.2 → 1.13.3
  - CVE-2024-XXXX: Request smuggling vulnerability
  - 영향: 모든 HTTP 요청
  - 수정: `npm update axios`

### Medium
{목록}

### Low
{목록}

## 📦 오래된 패키지

### 즉시 업그레이드 가능 (Patch)
| 패키지 | 현재 | 최신 | 명령어 |
|--------|------|------|--------|
| axios | 1.13.2 | 1.13.3 | `npm update axios` |
| vite | 5.4.2 | 5.4.5 | `npm update vite` |

### 신중 업그레이드 (Minor)
| 패키지 | 현재 | 최신 | Breaking Changes | 테스트 필요 |
|--------|------|------|------------------|-------------|
| react | 18.3.1 | 18.4.0 | ❌ 없음 | ✅ 권장 |
| recharts | 3.5.1 | 3.6.0 | ⚠️ 일부 API 변경 | ✅ 필수 |

### 계획 업그레이드 (Major)
| 패키지 | 현재 | 최신 | Breaking Changes | 마이그레이션 시간 |
|--------|------|------|------------------|-------------------|
| typescript | 5.5.3 | 6.0.0 | ✅ 많음 | 2-3일 |

## 🗑️ 사용하지 않는 패키지

- **lodash** (0KB) - 어디에도 import되지 않음
  - 제거 권장: `npm uninstall lodash`

## 📊 의존성 통계

- **총 dependencies**: 15개
- **총 devDependencies**: 12개
- **업데이트 가능**: 8개
- **보안 이슈**: 2개
- **사용하지 않음**: 1개

## 📋 업그레이드 계획

### Phase 1: 즉시 (오늘)
보안 패치 및 patch 버전 업데이트

\`\`\`bash
# 보안 패치
npm update axios
npm update vite

# 자동 테스트
npm run build
npm run typecheck
npm test
\`\`\`

**예상 시간**: 30분
**위험도**: ⬛⬜⬜⬜⬜ 낮음

### Phase 2: 이번 주 (신중)
Minor 버전 업데이트

\`\`\`bash
# React 업그레이드
npm install react@18.4.0 react-dom@18.4.0
npm install -D @types/react@18.4.0 @types/react-dom@18.4.0

# 테스트
npm run dev
# 수동 테스트: 모든 페이지 확인
npm run build
\`\`\`

**예상 시간**: 2시간 (테스트 포함)
**위험도**: ⬛⬛⬜⬜⬜ 중간

### Phase 3: 다음 분기 (계획)
Major 버전 업그레이드

\`\`\`bash
# TypeScript 6.0 업그레이드
npm install -D typescript@6.0.0

# 마이그레이션 가이드 확인
# Breaking changes 대응
# 전체 프로젝트 리팩토링
\`\`\`

**예상 시간**: 2-3일
**위험도**: ⬛⬛⬛⬛⬜ 높음

## ⚠️ 호환성 주의사항

### React 18.4.0
- `createRoot` API 변경
- `render` deprecation 경고
- 마이그레이션 필요: `src/main.tsx`

### Vite 5.4.5
- 플러그인 API 변경 없음
- 빌드 성능 개선 (+10%)
- 안전하게 업그레이드 가능

## ✅ 실행 체크리스트

- [ ] npm audit 결과 확인
- [ ] 보안 패치 적용
- [ ] 로컬에서 빌드 테스트
- [ ] 타입 체크 통과
- [ ] 단위 테스트 통과
- [ ] E2E 테스트 통과
- [ ] package-lock.json 커밋
- [ ] CHANGELOG.md 업데이트
- [ ] 팀에 알림

## 📝 다음 검사 일정
- 다음 주간 검사: {7일 후}
- 다음 월간 검사: {30일 후}
```

검사를 시작하세요!
